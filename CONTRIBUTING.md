# 参与贡献

感谢你帮助改进弈路助手。这个项目首先服务于“能直接照着操作”的体验，因此一条完整、可核验的过渡建议通常比多增加一个阵容名字更有价值。

## 开始之前

- 页面问题：使用 Bug 模板。
- 当前数据错误：使用数据纠错模板。
- 新阵容建议：使用新阵容模板，并先准备至少两个独立公开来源。
- 安全问题：不要公开提交 Issue，按 [SECURITY.md](SECURITY.md) 报告。
- 大范围界面或数据结构变更：建议先建 Issue 对齐范围。

## 本地开发

```bash
npm install
npm run dev
```

提交前至少运行：

```bash
npm run check
npm run build
npm run qa
```

不要提交 `node_modules/`、`dist/`、本地日志、账号凭据或抓取缓存中的个人信息。

## 贡献类型

### 程序与界面

- 保持手机和桌面端都可操作。
- 普通正文不要依赖过小字号；关键动作必须能快速扫读。
- 棋子头像、职责标签、名称和装备应使用相互独立的布局层，避免裁切。
- 外部数据不得未经转义直接进入 `innerHTML`。
- 不接入游戏客户端或实时对局数据。
- 不引入远程运行脚本、追踪器或非必要联网依赖。

### 数据纠错

请提供：

- 适用补丁、地区和段位范围。
- 当前页面内容与建议修正内容。
- 原始来源链接、页面更新时间和样本口径。
- 该修改属于统计更新，还是需要人工判断的运营建议。

同一指标在不同来源中冲突时，保留各自数值和口径，不取平均数伪造统一结论。

### 新阵容

新阵容进入正式区前必须具备：

- 至少两个相互独立、可公开访问且适用当前补丁的来源。
- 明确的最终阵容、主 C、主坦、装备方向和适合开局。
- 从开局到成型的阶段棋盘、人口、经济、D 牌开始与停止条件。
- 必留牌、临时牌、装备代持人、换装时点和替代牌。
- 条件不满足时的转阵方向。
- 成型后的升人口、替换顺序和站位建议。

自动发现的新阵容只能先创建候选 Issue 或 Draft PR。机器生成的过渡与装备建议不能未经人工复核直接发布。

### 素材

提交任何图片、图标、字体或音频时，必须同时更新 [ASSET_SOURCES.md](ASSET_SOURCES.md)，写清：

- 文件名和用途。
- 原始链接或权利人。
- 获取日期。
- 许可、政策条款或其他使用依据。
- 是否允许仓库再分发和修改。

不能确认权利状态的素材不得进入公开发布构建。不要绕过登录、验证码、访问控制或反爬措施获取素材。

## Pull Request 要求

- 一个 PR 尽量只解决一个主题。
- 说明用户可见变化和未覆盖范围。
- 数据 PR 附来源；视觉 PR 附桌面和手机截图。
- 勾选实际执行过的检查，不要把“命令运行过”写成“页面已人工验证”。
- 不顺手重构无关代码，也不要覆盖其他贡献者正在进行的修改。

维护者会重点检查：来源独立性、补丁一致性、过渡是否可执行、素材权利状态、移动端可读性和失败回退。

## 内容表达

- 使用简洁中文，优先写动作、条件和停止点。
- 不写“必吃分”“必吃鸡”等保证性描述。
- 明确区分公开数据、来源观点、项目整理建议和待确认事项。
- 引用来源时做摘要和数据记录，不大段复制第三方攻略文字。

