# 安全政策

## 项目状态

弈路助手 v7 尚未公开发布。正式上线后，仅维护最新公开版本；历史预览版可能不再接受安全更新。

## 报告安全问题

请优先使用 GitHub 仓库的 **Private vulnerability reporting / Security advisory** 私下报告。维护者账号或专用安全邮箱尚未确定前，不要在公开 Issue 中披露可利用细节。

报告中请包含：

- 受影响的提交、版本或页面。
- 可复现的最小步骤。
- 实际影响和可能的攻击路径。
- 如有可行方案，可附修复建议。

请不要提交真实账号、Cookie、访问令牌、密钥或其他人的个人数据。维护者确认报告后会先评估和限制影响，再决定修复与披露时间。

## 安全边界

弈路助手设计为静态网页：

- 不接入、读取或修改游戏客户端。
- 不读取实时对局、商店、对手、战绩或 Riot 账号数据。
- 不要求用户输入 Riot、腾讯或第三方网站凭据。
- 不应在浏览器端保存构建、抓取或部署密钥。

任何改变这些边界的 PR 都需要单独安全评审，不能作为普通功能顺带合入。

## 重点风险

- 自动来源中的文字或 URL 被用于内容注入或 XSS。
- GitHub Actions 权限过大、依赖未固定或工作流可被不受信任 PR 利用。
- 抓取任务意外记录 Cookie、响应头或其他敏感数据。
- 第三方依赖或构建脚本被污染。
- 公开页面引入跟踪器、远程脚本或不必要的网络请求。
- 素材文件包含异常载荷、隐私信息或未经授权内容。

## 维护要求

- 外部输入按字段白名单解析，所有展示文本安全转义。
- 不把自动抓取的 HTML 直接写入页面，也不通过不可信字符串设置 `innerHTML`。
- GitHub Actions 使用最小权限；部署任务与抓取任务分离。
- 不使用 `pull_request_target` 运行来自分支的未审查代码。
- 第三方 Action 固定到明确版本或提交，并定期审查。
- Secrets 只保存在 GitHub 提供的秘密存储中，不写入仓库、日志或前端包。
- 自动更新异常时停止发布并保留上一份可靠构建。
- 公共页面默认不加入分析追踪；以后若确需统计，应先说明数据范围和隐私影响。

## 不属于安全漏洞的内容

阵容强度判断、装备建议、错别字或统计口径分歧通常属于数据质量问题，请使用数据纠错模板。游戏版本变化造成的攻略过期也不是安全漏洞。

